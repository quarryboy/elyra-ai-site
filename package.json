{
  "name": "elyra-ai-site",
  "homepage": "https://quarryboy.github.io/elyra-ai-site/",
  "private": true,
  "version": "1.28.0",
  "license": "Apache 2.0",
  "scripts": {
    "dev": "gatsby develop -H 0.0.0.0",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "dev:clean": "gatsby clean && gatsby develop -H 0.0.0.0",
    "build": "gatsby build",
    "build:clean": "yarn clean && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "classnames": "^2.3.1",
    "gatsby": "^2.32.13",
    "gatsby-theme-carbon": "^1.28.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
